{
  "name": "human-tribunal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}
